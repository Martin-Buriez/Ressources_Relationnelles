<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
        content="width=device-width, user-scalable=yes, initial-scale=1.0, maximum-scale=6.0, minimum-scale=1.0">
    {% block description %}
        <meta name="description" content="Plateforme de création et de partage rapide de blog, simplicité et communauté.">
    {% endblock %}
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <!-- Font awesome -->
    <script src="https://kit.fontawesome.com/4e44dc3df4.js" crossorigin="anonymous"></script>
    <title>{% block title %}Administration - Tail-Blog{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascript %}
    {{ encore_entry_script_tags('app') }}
{% endblock %}
</head>
<body class="bg-admin-blue text-white">
<header class="sticky top-0 z-50">
    {% include 'partials/admin/header.html.twig' %}
</header>
<div class="flex justify-between w-screen">
    {% include 'partials/admin/sidebar.html.twig' %}
    <main class="relative bg-main-white dark:bg-main-black min-h-screen w-10/12 ml-[4.15rem] md:ml-[8rem] lg:ml-[15rem] xl:ml-[26rem] p-4 md:p-8 lg:p-12">
        {% block body %}{% endblock %}
    </main>
</div>
</body>
{% block javascripts %}
    <script>
        function closeAlert(event){
            let element = event.target;
            while(element.nodeName !== "BUTTON"){
                element = element.parentNode;
            }
            element.parentNode.parentNode.removeChild(element.parentNode);
        }
    </script>
{% endblock %}
</html>