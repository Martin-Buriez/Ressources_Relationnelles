{% extends 'Admin/admin_base.html.twig' %}

{% block title %}Les Catégories{% endblock %}

{% block body %}
    <div class="bg-main-white pt-3 mb-4">
        <div class="rounded-tl-3xl bg-gradient-to-r from-blue1 to-gray-600 p-4 shadow text-2xl text-white">
            <h1 class="font-bold pl-2">Gestion des catégories </h1>
        </div>
    </div>
    <div class="w-5/12 overflow-x-auto text-black">
        {{ form_start(categoryForm) }}
        <div class="mb-4 mt-5">
            <div class="mb-4 md:mb-0 ">
                <label class="block mb-2 font-bold text-main-grey" for="blog">
                    {{ form_row(categoryForm.title) }}
                </label>
            </div>
        </div>
        <div class="w-full">
            <div class="mb-6 text-center ">
                {{ form_row(categoryForm.submit) }}
            </div>
        </div>
        {{ form_end(categoryForm) }}
    </div>
    <div class="mt-12 lg:mt-20 xl:mt-24 mx-4">
        <div class="w-full overflow-hidden rounded-lg shadow-xs">
            <div class="w-full overflow-x-auto">
                <table class="w-full ">
                    <thead>
                        <tr class="font-extrabold tracking-wide text-left uppercase border-b bg-blue1 text-black dark:text-white">
                            <th class="px-4 py-3">ID de la Catégorie</th>
                            <th class="px-4 py-3">Titre</th>
                            <th class="px-4 py-3">Action</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-700 bg-gray-800">
                    {% for category in categorys %}
                        <tr class="hover:bg-white bg-blue2 text-black">
                            <td class="px-4 py-3">
                                <a target="_blank" rel="noreferrer noopener" href="#">
                                    {{ category.id }}
                                </a>
                            </td>
                            <td class="px-4 py-3">
                                {{ category.title }}
                            </td>
                            <td class="px-4 py-3 text-black">
                                <a onclick="confirm('Supprimer la catégorie et tout ses articles ?')" class="ml-8" href="#"><i class="text-orange-300 fas fa-trash mr-1"></i>Supprimer</a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}
